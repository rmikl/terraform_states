#IN rmiklas
# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- script: |
    #curl -fsSL https://apt.releases.hashicorp.com/gpg | sudo apt-key add -
    #sudo apt-add-repository "deb [arch=$(dpkg --print-architecture)] https://apt.releases.hashicorp.com $(lsb_release -cs) main"
    #sudo apt install -y terraform 
    #curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
    #az login --service-principal -u $(GITHUB_APPID) -p $(GITHUB_PASSWORD) --tenant $(GITHUB_TENANT)
    #az account show | grep -i robert
- script: |
    echo test_file > test_file
    echo "$(TFSTATES_TOKEN)" | az devops login --organization https://robertm131.visualstudio.com/ --verbose 
    az artifacts universal publish --organization https://robertm131.visualstudio.com/ --project="terraform_azure" --scope project --feed tfstates --name my-first-package --version 0.0.1 --description "Welcome to Universal Packages" --path $(PWD)/test_file
  displayName: 'test uplaod'
